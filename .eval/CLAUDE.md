# CLAUDE.md — large-content-studio

Orientation for an AI agent working in **this** repository. Generated by the evaluation pipeline
(PR #0) from the repository itself — every statement below is grounded in this codebase.

## Build & verify

Detected stack: **node** (npm). Commands verified green at commit `fec39dcd9f45b6abe1fbd397b5a474922a0719bd`:

- **install:** `npm ci`
- **build:** _none_
- **analyze:** _none_
- **test:** _none_

## Identifiers

Every user-facing component carries a stable, path-derived comment identifier of the form `// @uxid <repo-relative-path-without-ext>::<ComponentName>` (e.g. `// @uxid src/components/Matrix::Matrix`). These are permanent anchors: additive-only, never renamed. See .eval/components.md for the full inventory.

(18 component identifier(s) applied — see .eval/components.md.)

## Off-limits — never modify

- `*.g.dart`
- `*.freezed.dart`
- `.github/`
- `infra/`

## Conventions

- Encapsulate each workflow's data-fetching and AI-call logic in a dedicated feature hook that owns local result state, keeping presentational components thin.
- Manage component-local UI and result state with useState hooks rather than external state containers.
- Export presentational feature and layout components as named function components (reserve default export for the App root).
- Define explicit TypeScript interfaces or types for component props and shared shapes.
- Model closed sets of options (tabs, tones, languages) as typed constant arrays or union types kept alongside their feature.
- Wrap native form controls in shared UI primitives that spread through standard HTML element props.

_Full examples in .eval/architecture/guidelines.md._

## Knowledge base

See **.eval/INDEX.md** for the full map (architecture, design system, business context).
