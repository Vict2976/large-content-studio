# Module: client

Vite build configuration for React frontend client with API proxy to localhost:4000

## Key files
- `client/vite.config.ts` — Vite build config defining React plugin setup and development server proxy

## Public interface
- vite configuration export

## Dependencies
- vite
- @vitejs/plugin-react

## Open questions
- What APIs does the frontend consume from the /api endpoint?
- What is the production build target and deployment environment?
- Are there environment-specific configurations for staging/production?
